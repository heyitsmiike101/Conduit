# Conduit backend dependencies
# Pin to minimum compatible versions. Upgrade intentionally, not automatically.

# --- Web framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# --- Database ---
sqlalchemy>=2.0.0

# --- Configuration & validation ---
pydantic>=2.0.0
pydantic-settings>=2.0.0

# --- Scheduling ---
apscheduler>=3.10.0

# --- Encryption ---
cryptography>=42.0.0

# --- System metrics ---
psutil>=5.9.0

# --- HTTP client (used by helper integration & internal calls) ---
httpx>=0.27.0

# --- Cron utilities ---
croniter>=2.0.0
cron-descriptor>=1.4.0

# --- Form parsing (required by FastAPI for multipart uploads) ---
python-multipart>=0.0.9
