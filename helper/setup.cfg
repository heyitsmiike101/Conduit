[metadata]
name = conduit-helper
version = 0.1.0
description = Helper library for Conduit-managed Python scripts

[options]
packages = find:
install_requires =
    httpx>=0.24
python_requires = >=3.9
